/**
 * In this environment, the local urls are configured, with an api url for the API.
 */
export const environment = {
  production: false,
  hmr       : false,
  url_api: 'http://localhost:3000/api/'
};
